spring.application.name=Flipr-website


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# SMART DATABASE CONNECTION
# This tells Java: "If there is a cloud database URL, use it. If not, use localhost."
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/portfolio_db}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:naina-manghani12345}

# Server Port (Railway sets the PORT variable automatically)
server.port=${PORT:8094}