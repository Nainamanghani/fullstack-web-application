spring.application.name=Flipr-website

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# DATABASE (Railway + Local both work)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/portfolio_db}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:naina-manghani12345}

# PORT (Railway provides PORT automatically)
server.port=${PORT:8094}


